# 📝 Ubuntu 24.04 系统维护与内核同步笔记

### 1. 核心任务：同步 HWE 内核轨道

**背景**：从旧版本（如 22.04）升级而来的系统，默认可能留在旧的内核映射路径上。
**操作**：手动切换到 24.04 专用的硬件启用（HWE）通道，使其能够自动获取最新的主线内核（如 6.13+）。

```bash
# 安装 24.04 HWE 核心包
sudo apt update
sudo apt install linux-generic-hwe-24.04 -y

```

### 2. 常用版本检查命令

* **查看当前运行内核**：`uname -r`
* **查看系统详细信息**：`hostnamectl`
* **查看已安装内核列表**：`dpkg --get-selections | grep linux-image`

### 3. 跨版本升级后的“大扫除”

升级后会残留旧版本的配置文件和孤立包，需定期执行深度清理以释放 `/boot` 分区空间：

```bash
# 1. 清除标记为 deinstall 的旧配置残留 (rc 状态)
sudo apt purge $(dpkg -l | grep '^rc' | awk '{print $2}')

# 2. 自动移除不再需要的旧内核及依赖包
sudo apt autoremove --purge -y

# 3. 清理下载缓存
sudo apt clean

```

### 4. 硬件级维护 (针对 HP 服务器)

利用 `fwupd` 管理服务器底层的固件（BIOS/网卡/阵列卡等）：

* **安装**：`sudo apt install fwupd -y`
* **更新流程**：
1. `sudo fwupdmgr refresh` (刷新列表)
2. `sudo fwupdmgr get-updates` (检查更新)
3. `sudo fwupdmgr update` (执行升级)



### 5. 软件源与第三方应用 (Docker/Tailscale)

跨版本升级后，务必确认第三方仓库的代号已从 `jammy` (22.04) 变更为 `noble` (24.04)。

* **检查路径**：`/etc/apt/sources.list.d/`
* **操作**：若发现旧代号，修改后执行 `sudo apt update`。

### 6. 专家级“一键更新”别名 (Alias)

建议将以下命令加入您的 `~/.bashrc`，实现专业级的全面维护：

```bash
alias sys-update='sudo apt update && sudo apt full-upgrade -y && sudo apt autoremove --purge -y && sudo apt clean && sudo snap refresh'

```

---

**💡 维护提示：**
由于内核更新涉及底层架构变动，每次看到 `linux-image` 更新后，请务必安排时间 **重启系统** (`sudo reboot`) 以启用新内核。