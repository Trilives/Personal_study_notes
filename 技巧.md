# windows下创建无后缀应用
按住 `Shift` + 右键文件夹空白处 → 选择 **“在此处打开 PowerShell 窗口”** 或 **“终端”**，然后运行：
```
# PowerShell 创建空文件
New-Item "myfile.yaml"
New-Item ".env"
New-Item "Dockerfile"
```
或使用 CMD：
```
type nul > config.yaml
copy nul .env
```